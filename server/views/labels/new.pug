extends ../layouts/application

include ../mixins/input
include ../mixins/submit

block content
  form(action=route('labels'), method="post")
    +input(label, 'name', errors, 'text')
    +submit(t('views.labels.new.submit'))